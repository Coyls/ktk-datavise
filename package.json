{
  "name": "ktk-data-visualisation",
  "version": "1.0.0",
  "description": "",
  "source": "./public/index.html",
  "scripts": {
    "build": "rm -rf ./dist && parcel build",
    "start": "node .",
    "dev": "parcel ./public/index.html --no-hmr --no-cache --no-source-maps",
    "cd": "rm -rf ./dist && git add .",
    "cr": "npm run build && git add ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@turf/center": "^6.5.0",
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "swiper": "^7.3.1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "cz-conventional-changelog": "^3.3.0",
    "parcel": "^2.0.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
